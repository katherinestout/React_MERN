//fetching profiles


const initialState = {
    profile: null,
    profiles: null,
    loading: false
}

export default function( state = initialState, action){
    //switch statement based on type
switch(action.type) {
    default:
    return state;
}
}